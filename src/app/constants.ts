import {Insets, Platform} from 'react-native';

export const DEFAULT_USERS_LIMIT = 15;
export const IS_IOS = Platform.OS === 'ios';
export const HIT_SLOP_10: Insets = {top: 10, right: 10, bottom: 10, left: 10};
